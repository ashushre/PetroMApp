export class TransporterProfile {
    id: number;
    name: string;
    pumpId: number;
    transporterId: number;
    address: string;
    PINCode: string;
    mobileNo: string;
    email: string;
    bankName: string;
    branchAddress: string;
    accountNo: string;
    IFSCCode: string;
    username: string;
    updated_by: string;
    status: number;
    countryId:number;
    cityId:number;
    regionId:number;
}
// 'transporters.address', 'transporters.PINCode', 'transporters.email', 'transporters.accountNo', 'transporters.bankName', 
//         'transporters.branchAddress', 'transporters.IFSCCode'
// 'transporters.countryId', 'transporters.regionId', 'transporters.cityId', 'transporters.address', 'transporters.PINCode', 'transporters.email', 'transporters.accountNo', 'transporters.bankName', 
//         'transporters.branchAddress', 'transporters.IFSCCode'